diff --git a/oci/private/BUILD.bazel b/oci/private/BUILD.bazel
index a432341..9cf1cc9 100644
--- a/oci/private/BUILD.bazel
+++ b/oci/private/BUILD.bazel
@@ -52,6 +52,15 @@ bzl_library(
     ],
 )
 
+# package_metadata doesn't have any bzl_library targets
+# so one is made here.
+bzl_library(
+    name = "package_metadata",
+    srcs = [
+        "@package_metadata//rules:srcs",
+    ],
+)
+
 bzl_library(
     name = "pull",
     srcs = ["pull.bzl"],
@@ -60,8 +69,10 @@ bzl_library(
         "//oci:__subpackages__",
     ],
     deps = [
+        ":package_metadata",
         "//oci/private:authn",
         "//oci/private:util",
+        "@aspect_bazel_lib//lib:copy_to_directory",
         "@bazel_skylib//lib:dicts",
     ],
 )
diff --git a/oci/private/pull.bzl b/oci/private/pull.bzl
index 84617ce..8e1968b 100644
--- a/oci/private/pull.bzl
+++ b/oci/private/pull.bzl
@@ -1,10 +1,16 @@
 "Implementation details for oci_pull repository rules"
 
+load("@aspect_bazel_lib//lib:copy_to_directory.bzl", _copy_to_directory = "copy_to_directory")
 load("@bazel_skylib//lib:dicts.bzl", "dicts")
 load("@bazel_skylib//lib:versions.bzl", "versions")
+load("@package_metadata//rules:package_metadata.bzl", _package_metadata = "package_metadata")
 load("//oci/private:authn.bzl", "authn")
 load("//oci/private:util.bzl", "util")
 
+# Reexports for bzlmod resolution in generated `oci_pull` repositories.
+copy_to_directory = _copy_to_directory
+package_metadata = _package_metadata
+
 # attributes that are specific to image reference url. shared between multiple targets
 _IMAGE_REFERENCE_ATTRS = {
     "scheme": attr.string(
@@ -187,8 +193,7 @@ def _create_downloader(rctx, authn):
 _BUILD_FILE_TMPL = """\
 "Generated by oci_pull. DO NOT EDIT!"
 
-load("@aspect_bazel_lib//lib:copy_to_directory.bzl", "copy_to_directory")
-load("@package_metadata//rules:package_metadata.bzl", "package_metadata")
+load("@rules_oci//oci/private:pull.bzl", "copy_to_directory", "package_metadata")
 
 package_metadata(
     name = "package_metadata",
